<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog!</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/jass.css">
  

    <link rel="stylesheet" href="/css/style.css">

</head>

<body>
<div class="clr">
    <ul class="nav display-flex  p-2">
        <li class="nav-item">
            <a class="nav-link active no-button" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
            <a class="nav-link no-button" href="/dashboard">Dashboard</a>
        </li>
        <li class="nav-item">
            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            {{!-- <a href="/profile">profile</a> | --}}
            {{!-- <button class="no-button" id="logout">logout</button> --}}
            <a class="nav-link no-button" id="logout">logout</a>
            {{else}}
            <a class="nav-link no-button"  href="/login">login</a>
            {{/if}}
        </li>
    </ul>
    <h1>
        <a href="/">The Tech Blog!</a>
    </h1>
</div>
    <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>
    <footer>

    </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>